<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="knowyc.com.knowyc" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme" >
        <activity
            android:name=".activities.SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ActivityLogin"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.ChatActivity"
            android:label="@string/title_activity_chat" >
        </activity>
        <activity
            android:name=".activities.FormActivity"
            android:label="@string/title_activity_form" >
        </activity>
        <activity
            android:name=".activities.selectactivity"
            android:label="@string/title_activity_selectactivity" >
        </activity>
        <activity
            android:name=".activities.MyActivity"
            android:label="MyActivity" >
        </activity>
        <activity
            android:name=".activities.SmsActivity"
            android:label="SmsActivity" >
        </activity>
        <activity
            android:name=".activities.UpdateAddressActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.DocumentsSubmissionActivity"
            android:label="@string/title_activity_documents_submission"
            android:theme="@style/MyMaterialTheme" >
        </activity>

        <!-- Intent service -->
        <service
            android:name=".service.HttpService"
            android:exported="false" />
    </application>

</manifest>
